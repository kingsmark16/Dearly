services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: dearly
      POSTGRES_USER: dearly
      POSTGRES_PASSWORD: dearly
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - dearly-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U dearly -d dearly']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - '127.0.0.1:6379:6379'
    volumes:
      - dearly-redis:/data
    command: ['redis-server', '--appendonly', 'yes']
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  mailpit:
    image: axllent/mailpit:latest
    ports:
      - '127.0.0.1:1025:1025'
      - '127.0.0.1:8025:8025'

volumes:
  dearly-postgres:
  dearly-redis:
